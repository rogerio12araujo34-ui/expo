import React from 'react';
import { View, Text, TextInput, TouchableOpacity, ScrollView, SafeAreaView } from 'react-native';
import { Mail, Zap, Droplet, Paintbrush, Hammer, Search } from 'lucide-react-native';

const ServiceCard = ({ icon: Icon, label, color }) => (
  <TouchableOpacity className="bg-white p-4 rounded-2xl m-2 w-[44%] shadow-sm items-center border border-gray-100">
    <View className={`p-3 rounded-full ${color} mb-2`}>
      <Icon size={28} color="white" />
    </View>
    <Text className="font-bold text-gray-800">{label}</Text>
    <Text className="text-xs text-gray-400">Solicitar agora</Text>
  </TouchableOpacity>
);

export default function HomeScreen() {
  return (
    <SafeAreaView className="flex-1 bg-gray-50">
      <ScrollView className="px-5">
        
        {/* Header */}
        <View className="mt-8 mb-6">
          <Text className="text-2xl font-bold text-blue-600">Tonaporta</Text>
          <Text className="text-gray-500">O que você precisa consertar hoje?</Text>
        </View>

        {/* Barra de Busca */}
        <View className="flex-row items-center bg-white p-4 rounded-xl shadow-sm border border-gray-100 mb-6">
          <Search size={20} color="#9CA3AF" />
          <TextInput 
            placeholder="Ex: Trocar chuveiro, Pintura..." 
            className="ml-3 flex-1 text-gray-700"
          />
        </View>

        {/* Seção de Categorias */}
        <Text className="text-lg font-bold text-gray-800 mb-4 ml-2">Nossos Serviços</Text>
        
        <View className="flex-row flex-wrap justify-between">
          <ServiceCard icon={Zap} label="Elétrica" color="bg-yellow-400" />
          <ServiceCard icon={Droplet} label="Encanador" color="bg-blue-500" />
          <ServiceCard icon={Paintbrush} label="Pintura" color="bg-purple-500" />
          <ServiceCard icon={Hammer} label="Pedreiro" color="bg-orange-600" />
          <ServiceCard icon={Mail} label="Montagem" color="bg-green-500" />
          <ServiceCard icon={Search} label="Outros" color="bg-gray-400" />
        </View>

        {/* Banner de Urgência */}
        <TouchableOpacity className="bg-red-500 p-5 rounded-2xl mt-6 flex-row items-center justify-between shadow-lg">
          <View>
            <Text className="text-white font-bold text-lg">SOS Emergência</Text>
            <Text className="text-white opacity-80">Vazamentos ou Curto-circuito</Text>
          </View>
          <Text className="text-white text-3xl">🚨</Text>
        </TouchableOpacity>

      </ScrollView>
    </SafeAreaView>
  );
}
